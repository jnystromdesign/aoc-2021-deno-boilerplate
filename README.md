#Boilerplate for Advent of Code - Deno 

## Usage: 

0. Install deno `brew install deno`
1. Add your input in `input.txt` 
2. Do your data massaging in `index.ts solveQuestion`.
3. Run `deno run --allow-read --watch index.ts`
